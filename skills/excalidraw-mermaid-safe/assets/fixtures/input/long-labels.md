# Diagram With Long Labels

## Implementation Delta
- This single item intentionally contains an overlong label that should be truncated before it becomes unreadable inside Excalidraw imports and screenshots.
- Use stable wrapper commands for repeatable validation.
