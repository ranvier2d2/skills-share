interface:
  display_name: "Overlay Browser Navigator"
  short_description: "Navigate websites with overlay semantic signals"
  brand_color: "#2563EB"
  default_prompt: "Use $overlay-browser-navigator to navigate this website by ranking targets with browser and overlay signals."

policy:
  allow_implicit_invocation: true

